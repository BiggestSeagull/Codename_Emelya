using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GuitarPuzzle : PuzzleSystem
{
    // Start is called before the first frame update
    void Start()
    {
        Debugging();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
